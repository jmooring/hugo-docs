---
_comment: Do not remove front matter.
---

## Options

path
: (`string`) The [logical path](g) to the page, relative to the `content` directory. Required.

lang
: (`string`) The language of the page. Default is the current language. Optional.

outputFormat
: (`string`) The output format of the page. Default is the current output format. Optional.
