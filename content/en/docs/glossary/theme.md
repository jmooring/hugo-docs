---
title: theme
---

A _theme_ is a [_module_](g) that provides a complete set of [_components_](g) to define a site's layout, presentation, and behavior. While every theme is a module, not every module is a theme.
